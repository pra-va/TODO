import { NodesListState, NodeActionType } from './types'
import { CREATE, DELETE } from './actionTypes'

const initialState: NodesListState = {
   nodesList: [] 
}

export const reducer = 
   (state = initialState, action: NodeActionType): NodesListState => {
      switch (action.type) {
         case
      }
}