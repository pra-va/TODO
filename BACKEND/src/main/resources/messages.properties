# Fields validation messages.

# 

# Generic parts of the messages.
length.between=has to be between {min} and {max} characters long
required=is required.

# Field-specific messages.
username.size=Username {length.between}.
username.required=Username {required}.
password.size=Password {length.between}.
password.required=Password {required}.
email.format=Email format is not valid.
email.required=Email {required}.
